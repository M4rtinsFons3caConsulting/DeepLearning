# Packaging

- Create the packaging for utility functions using .toml architecture #DONE

# Preprocessing

- Flatten the directory structure to conforme with flow from dataframe workflow; #DONE

- Change file_names to metadata_tas <eol_content_id,eol_page_id>.jpg #DONE

- Update the metadata.csv: 
    - with updated filenames #DONE
    - with binary_labels.    #DONE

- Create a script to deduce the class imbalance in the difference labelling schema i.e. binary, multi-class;

- Create naming convention for scheduled transformations.

- Build and implement the binary_labeller expansion with random background cropping to increase the collection of images with  non-animal labels.

- Update the metadata.csv with the resulting image lines;

- Oversample the dataset ahead of time using, in accordance with the deduced class imbalances:
    . 90º rotation, left and right;
    . x-axis inversion;
    . increase or decrease saturation;
    . increase or decrease brightness;
    . manipulate single channel hue;

    and, apply the aforementioned naming convention to the resulting images;

- Create a function to read from an auxiliary metadata file, and return a random

# Architecture
- <Here the architectural deployment considerations>

# Benchmarking
- <Here the banchmarking procedure>